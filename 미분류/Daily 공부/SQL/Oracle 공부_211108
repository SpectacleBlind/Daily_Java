SELECT

ORACLE 상위 N개 레코드


SELECT NAME
FROM (SELECT *
     FROM ANIMAL_INS
     ORDER BY DATETIME)
     WHERE ROWNUM <2;
	 
	 
	 
ORACLE 여러 기준으로 정렬하기

SELECT ANIMAL_ID, NAME, DATETIME // 뭐 보고 싶은지 결정
FROM ANIMAL_INS
WHERE NAME IS NOT NULL //
ORDER BY NAME ASC, DATETIME DESC // 오름차순 ASC 내림차순 DESC




ORACLE 동물의 아이디와 이름

SELECT ANIMAL_ID, NAME
FROM ANIMAL_INS
ORDER BY ANIMAL_ID


ORACLE 어린 동물 찾기

SELECT ANIMAL_ID, NAME
FROM ANIMAL_INS
WHERE INTAKE_CONDITION != 'Aged'  // IS NOT NULL 은 NULL 찾기, != 으로 찾을수도 있음
ORDER BY ANIMAL_ID


ORACLE 아픈 동물 찾기

SELECT ANIMAL_ID, NAME
FROM ANIMAL_INS
WHERE INTAKE_CONDITION = 'Sick'
ORDER BY ANIMAL_ID

ORACLE 이름있는 동물의 아이디

SELECT ANIMAL_ID
FROM ANIMAL_INS
WHERE NAME IS NOT NULL 
ORDER BY ANIMAL_ID



ORACLE 역순 정렬하기
SELECT NAME, DATETIME
FROM ANIMAL_INS
ORDER BY ANIMAL_ID DESC

ORACLE 이름 없는 동물의 아이디
SELECT ANIMAL_ID
FROM ANIMAL_INS
WHERE NAME IS NULL
ORDER BY ANIMAL_ID
